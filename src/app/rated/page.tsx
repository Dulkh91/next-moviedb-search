import RateClientPage from "./RatedClientPage";
export const metadata = {
  title: "rated",
  description: "The page rated we can rate the moives by guest session id",
};
const RatingPage = () => {
  return <RateClientPage />;
};

export default RatingPage;
